<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <artifactId>{{SystemArtifactId}}</artifactId>
    <packaging>jar</packaging>
    <name>{{AppDisplayName}} Application</name>

    <parent>
        <groupId>{{GroupId}}</groupId>
        <artifactId>{{ParentArtifactId}}</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <dependencies>
        <dependency>
            <groupId>{{GroupId}}</groupId>
            <artifactId>{{RoeArtifactId}}</artifactId>
            <version>${project.version}</version>
        </dependency>

        <!-- In-process integration testing (AbstractTest + the test profile).
             The system module gains a dependency on every service module as
             services are scaffolded, so its tests can boot any service via
             EmbeddedXVM. -->
        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.13.2</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <!-- EmbeddedXVM reaches into JDK internals that Java 17 blocks by
                 default, so any JVM that boots a service needs the add-opens
                 flags. That is BOTH the test JVM and the in-process runner, so
                 the flags live in one property (rumi.jvm.args, in the parent) —
                 two copies drift, and the failure when they do is an obscure
                 InaccessibleObjectException rather than anything that names the
                 flag it is missing. -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.2.5</version>
                <configuration>
                    <argLine>${rumi.jvm.args}</argLine>
                </configuration>
            </plugin>

            <plugin>
                <groupId>com.neeve</groupId>
                <artifactId>nvx-rumi-xar-maven-plugin</artifactId>
                <version>${nvx.rumi.management.version}</version>
                <dependencies>
                    <dependency>
                        <groupId>com.neeve</groupId>
                        <artifactId>nvx-rumi-controller</artifactId>
                        <version>${nvx.rumi.management.version}</version>
                    </dependency>
                    <dependency>
                        <groupId>com.neeve</groupId>
                        <artifactId>nvx-rumi</artifactId>
                        <version>${nvx.rumi.version}</version>
                    </dependency>
                    <dependency>
                        <groupId>jakarta.xml.bind</groupId>
                        <artifactId>jakarta.xml.bind-api</artifactId>
                        <version>2.3.2</version>
                    </dependency>
                    <dependency>
                        <groupId>org.glassfish.jaxb</groupId>
                        <artifactId>jaxb-runtime</artifactId>
                        <version>2.3.2</version>
                    </dependency>
                </dependencies>
                <executions>
                    <execution>
                        <id>Xar</id>
                        <phase>package</phase>
                        <goals>
                            <goal>xar</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <!-- Profile for building standalone distribution -->
        <profile>
            <id>dist</id>
            <dependencies>
                <dependency>
                    <groupId>com.neeve</groupId>
                    <artifactId>nvx-rumi</artifactId>
                    <version>${nvx.rumi.version}</version>
                    <classifier>sandbox-${arch}</classifier>
                    <type>tar.gz</type>
                </dependency>
            </dependencies>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-assembly-plugin</artifactId>
                        <version>3.6.0</version>
                        <executions>
                            <execution>
                                <id>standalone</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>single</goal>
                                </goals>
                                <configuration>
                                    <descriptors>
                                        <descriptor>src/assembly/standalone.xml</descriptor>
                                    </descriptors>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
        <!-- ./run-in-process.sh activates this. The execution is bound to a PHASE
             rather than invoked as `exec:exec` on the command line, because a CLI
             goal runs on EVERY project in the reactor: the script needs `-am` so
             the sibling service modules are built (that is what removes the need
             for `mvn install`), and the parent pom is then in the reactor too and
             has no exec configuration, so the build fails with "the parameter
             'executable' is missing" before anything starts.

             Keeping it in a profile that only this module declares means `-am`
             can pull the siblings in without them inheriting an execution that
             makes no sense for them. -->
        <profile>
            <id>in-process</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>exec-maven-plugin</artifactId>
                        <version>3.1.1</version>
                        <executions>
                            <execution>
                                <id>run-in-process</id>
                                <phase>process-classes</phase>
                                <goals>
                                    <goal>exec</goal>
                                </goals>
                            </execution>
                        </executions>
                        <configuration>
                            <!-- ${java.home}, not a bare `java`: exec resolves a
                                 bare name off PATH, where a JDK 8 rejects the
                                 add-opens flags outright. ${java.home} is the JVM
                                 Maven is running on, which is the one that just
                                 compiled this app. (This repo already documents the
                                 same PATH/JDK trap for javac in ci/.) -->
                            <executable>${java.home}/bin/java</executable>
                            <classpathScope>compile</classpathScope>
                            <!-- `exec:exec` and not `exec:java`, deliberately:
                                 exec:java runs in Maven's own JVM, which cannot be
                                 given the add-opens flags EmbeddedXVM needs, so the
                                 engine fails to start there. Forking also makes
                                 %classpath the REACTOR's classpath, which is what
                                 lets this run without `mvn install`.
                                 basedir: InProcessRun resolves conf/config.xml
                                 against it, and a forked JVM does not inherit
                                 Maven's notion of it. -->
                            <commandlineArgs>${rumi.jvm.args} ${rumi.inprocess.args} -Dbasedir=${project.basedir} -classpath %classpath {{AppPackageName}}.InProcessRun</commandlineArgs>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>
